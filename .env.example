# 配置模板（复制为 .env 并填写真实值；.env 已加入 .gitignore，不会进入仓库）

# OpenAI 兼容接口地址，需包含 API 版本前缀
PCA_BASE_URL=https://opencode.ai/zen/go/v1

# 模型名称
PCA_MODEL=deepseek-v4-flash

# API key
PCA_API_KEY=在此填写你的 API key

# 可选：会话存储目录，默认 ~/.pca/sessions
# PCA_SESSION_DIR=